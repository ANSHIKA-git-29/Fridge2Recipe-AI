import {AnimatePresence,motion} from 'framer-motion'; import IngredientChip from './IngredientChip'; export default function IngredientList({items,onRemove}){return <div className="flex min-h-10 flex-wrap gap-2"><AnimatePresence>{items.map(x=><motion.div initial={{scale:.7,opacity:0}} animate={{scale:1,opacity:1}} exit={{scale:.7,opacity:0}} key={x}><IngredientChip name={x} onRemove={()=>onRemove(x)}/></motion.div>)}</AnimatePresence></div>}
