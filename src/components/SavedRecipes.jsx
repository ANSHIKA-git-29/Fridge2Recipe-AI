import {useMemo,useState} from 'react'; import {FiSearch} from 'react-icons/fi'; import RecipeCard from './RecipeCard'; export default function SavedRecipes({recipes,onOpen,onDelete}){const[q,setQ]=useState('');const list=useMemo(()=>recipes.filter(r=>r.recipeName.toLowerCase().includes(q.toLowerCase())),[recipes,q]);return <main className="mx-auto min-h-[70vh] max-w-6xl px-5 py-12"><p className="text-sm font-bold uppercase tracking-[.2em] text-orange">Your kitchen library</p><h1 className="mt-2 font-serif text-5xl">Saved recipes</h1><div className="glass mt-8 flex items-center gap-2 rounded-xl px-4"><FiSearch/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full bg-transparent py-4" placeholder="Search your saved recipes" aria-label="Search saved recipes"/></div>{list.length?<div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{list.map(r=><RecipeCard key={r.id} recipe={r} onOpen={()=>onOpen(r)} onDelete={()=>onDelete(r.id)}/>)}</div>:<div className="py-20 text-center opacity-60">No saved recipes yet. Your next delicious idea will appear here.</div>}</main>}
