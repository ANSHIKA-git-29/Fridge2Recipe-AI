# Gemini API key. This file is read only by backend/server.js; never use VITE_ for secrets.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Development defaults
BACKEND_PORT=8787
FRONTEND_ORIGIN=http://localhost:5173

# Production (used by `npm start`)
# PORT=3000
# NODE_ENV=production
